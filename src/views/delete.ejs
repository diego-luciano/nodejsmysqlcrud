<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>NodeCRUD - <%- title %></title>
    <%- include('partials/scripts.ejs') %>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container py-4">
        <h2 class="font-weight-bold">Delete</h2>
        <hr>
        <h5 class="font-italic">Are you sure you want to delete this user?</h5>
        <div class="row mt-3">
            <div class="col-lg-12">
                <% for (let index = 0; index < persons.length; index++) { %>
                    <div>
                        <p><span class="font-weight-bold">Full name:</span> <%- persons[index].first_name %> <%- persons[index].last_name %></p>
                        <p><span class="font-weight-bold">Address: </span> <%- persons[index].address %></p>
                        <p><span class="font-weight-bold">Position: </span> <%- persons[index].position %></p>
                        <p><span class="font-weight-bold">Salary: </span> <%- persons[index].salary %></p>
                    </div>
                    <form action="/delete/<%- persons[index].id %>" method="POST">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                <% }; %>
            </div>
        </div>
    </div>
</body>
</html>